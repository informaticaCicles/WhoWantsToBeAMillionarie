<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory
        android:title="@string/prefsCategory">

        <EditTextPreference
            android:key="user"
            android:title="@string/userTitle"
            android:summary="@string/userSummary"
            android:dialogTitle="@string/userDialogTitle"/>

        <EditTextPreference
            android:key="helps"
            android:title="@string/helpsTitle"
            android:summary="@string/helpsSummary"
            android:dialogTitle="@string/helpsDialogTitle"
            android:inputType="number"/>

    </PreferenceCategory>

</PreferenceScreen>